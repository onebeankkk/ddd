[package]
name = "test_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12.20", features = ["json"] }
tokio = { version = "1.45.1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
# 설정 파일 처리를 위해 추가
config = "0.14" 
# CSV 파일을 읽기위한 csv크리에이트 필요
csv = "1.3"

